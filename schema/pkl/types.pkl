/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

/// AWS-specific types used across resource schemas.
/// This module has no dependencies to avoid circular imports.
///
/// TODO: Once the deprecated formae.Tag alias is removed, move the Tag class
/// to aws.pkl which already imports formae.pkl. This will allow proper typing
/// (String|Mapping|formae.Value) instead of Any. At that point, this file can
/// be deleted. See docs/design/plugin-sdk-v1-phase2-plan.md for details.
module aws.types

/// AWS Tag - matches CloudFormation tag schema.
/// Used for resources that support tagging.
/// Accepts String or formae.Value for supporting resolvables (setOnce, opaque).
///
/// TODO: Value uses Any type as workaround during migration. Once formae.Tag
/// is removed, move this class to aws.pkl and restore proper typing:
/// value: String|Mapping|formae.Value
open class Tag {
    hidden key: String
    hidden value: Any  // TODO: Change to String|Mapping|formae.Value after migration

    fixed Key: String = key
    fixed Value: Any = value  // TODO: Change to String|Mapping|formae.Value after migration
}

/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

amends "@formae/forma.pkl"
import "@formae/formae.pkl"

import "@aws/aws.pkl"

import "@aws/ec2/vpc.pkl"
import "@aws/ec2/networkacl.pkl"

local testRunID = read("env:FORMAE_TEST_RUN_ID")
local stackName = "plugin-sdk-test-ec2-networkacl-\(testRunID)"

// VPC dependency - NetworkAcl needs a VPC
local testVpc = new vpc.VPC {
  label = "test-vpc-for-networkacl"
  cidrBlock = "10.0.0.0/16"
  enableDnsSupport = true
}

forma {
  new formae.Stack {
    label = stackName
    description = "Plugin SDK test for EC2 NetworkAcl"
  }

  new formae.Target {
    label = "aws-target"
    config = new aws.Config {
      region = "us-east-1"
    }
  }

  // VPC required for NetworkAcl
  testVpc

  // NetworkAcl under test
  new networkacl.NetworkAcl {
    label = "plugin-sdk-test-networkacl"
    vpcId = testVpc.res.vpcId
  }
}

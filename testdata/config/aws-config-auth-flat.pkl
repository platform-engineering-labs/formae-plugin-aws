/*
 * © 2025 Platform Engineering Labs Inc.
 *
 * SPDX-License-Identifier: FSL-1.1-ALv2
 */

amends "@formae/forma.pkl"
import "@formae/formae.pkl"

import "@aws/aws.pkl"

local testRunID = read("env:FORMAE_TEST_RUN_ID")
local stackName = "plugin-sdk-test-aws-config-auth-flat-\(testRunID)"

forma {
  new formae.Stack {
    label = stackName
    description = "Plugin SDK fixture: legacy flat profile, no auth block"
  }

  new formae.Target {
    label = "aws-target"
    config = new aws.Config {
      region = "us-east-1"
      profile = "legacy-profile"
    }
  }
}

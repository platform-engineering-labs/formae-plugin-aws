amends "pkl:Project"

dependencies {
  // Reference local plugin schema during development
  ["aws"] = import("../schema/pkl/PklProject")

  // Formae schema - fetched from public registry
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.83.0"
  }
  // In-repo examples resolve @aws against the LOCAL schema so they exercise this
  // plugin's current resource definitions (incl. the ECS resolvable patches the
  // bootstrap example needs), rather than a published hub version.
  ["aws"] = import("../schema/pkl/PklProject")
}
